# ============================================================
# mobile-automation —— 基于 Qwen-VL 的移动端 AI 自动化框架
# 安装方式：
#   pip install -r requirements.txt
#   uv pip install -r requirements.txt
# ============================================================

# ========== 核心依赖 ==========

# LLM 相关
openai>=2.44.0                  # Qwen DashScope 兼容接口 + OpenAI GPT-4o
anthropic>=0.112.0              # Claude API 调用

# 设备交互
uiautomator2>=3.7.0             # Android UI 自动化（主要交互方式）
adbutils>=2.12.0                # ADB 设备管理工具

# 图像处理
Pillow>=12.2.0                  # 图片缩放与格式转换
opencv-python>=4.13.0           # SSIM 视觉比较
scikit-image>=0.26.0            # structural_similarity

# XML 解析
lxml>=6.1.0                     # 快速 XML dump 解析

# 配置管理
pydantic>=2.13.0                # 数据模型
pydantic-settings>=2.14.0       # 环境变量配置管理

# 工具
python-dotenv>=1.1.0            # .env 文件加载

# ========== 开发依赖 ==========

pytest>=9.0.0                   # 测试框架
pytest-mock>=3.15.0             # mock 支持
pytest-asyncio>=1.4.0           # 异步测试
pytest-cov>=7.0.0               # 覆盖率报告
